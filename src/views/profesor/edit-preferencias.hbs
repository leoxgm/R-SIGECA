<div class="container text-center mb-3">
  <div class="card mx-auto mt-4" style="width: 62rem;">
    <div class="card-body">
      <h3>Hola, {{user.name}}</h3>
      <form action="/profesores/edit-preferencia" method="POST">
      {{#if listaPreferencias}}
        <h5>Edita tus preferencias</h5>
          <table class="table table-striped">
            <thead>
              <tr>
                <th scope="col">Seleccionar</th>
                <th scope="col">Clave</th>
                <th scope="col">UEA</th>
                <th scope="col">Descripción</th>
              </tr>
            </thead>
            <tbody>
              {{#each listaPreferencias}}
                <tr>
                  <th>
                    <input
                      class="form-check-input"
                      type="checkbox"
                      id="checkboxNoLabel{{clave}}"
                      value={{clave}}
                      name="uea"
                      aria-label="check_{{clave}}"
                      checked
                    />
                  </th>
                  <td>{{clave}}</td>
                  <td>{{uea}}</td>
                  <td>{{description}}</td>
                </tr>
              {{/each}}
              {{#each ListaUEA}}
                <tr>
                  <th>
                    <input
                      class="form-check-input"
                      type="checkbox"
                      id="checkboxNoLabel{{clave}}"
                      value={{clave}}
                      name="uea"
                      aria-label="check_{{clave}}"
                    />
                  </th>
                  <td>{{clave}}</td>
                  <td>{{uea}}</td>
                  <td>{{description}}</td>
                </tr>
              {{/each}}
            </tbody>
          </table>
          <div class="form-group">
            <button class="btn btn-success w-50 mt-3" type="submit">
              Guardar preferencias
            </button>
          </div>
      </form>
      <a href="/profesores" class="btn btn-primary mt-3 g-2 me-3">Volver</a>
      {{else}}
        <p>No existen UEAs registradas, hablar con el administrador.</p>
        <img
          src="/pictures/error.png"
          class="img-fluid mt-3 mb-3"
          alt="ERROR"
          width="340"
          height="240"
        />
      {{/if}}
    </div>
  </div>
</div>